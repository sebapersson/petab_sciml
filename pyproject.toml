[build-system]
requires = ["setuptools>=64", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "petab_sciml"
dynamic = ["version"]
authors = [
  {name = "Sebastian Persson"},
  {name = "Dilan Pathirana"},
  {name = "Branwen Snelling"},
]
dependencies = [
  "h5py>=3.12",
  "mkstd>=0.0.10",
  "numpy>=2.1",
  "pandas",
  "pydantic",
  "python-libsbml",
  "ruamel.yaml",
]
requires-python = ">=3.12"
description = "Specify parameter estimation problems and hybrid models."
readme = "README.md"
license = {file = "LICENSE"}
classifiers = [
  "Development Status :: 4 - Beta",
  "Programming Language :: Python"
]

[project.optional-dependencies]
doc = [
    "sphinx>=8.1.3",
    "sphinxcontrib-napoleon>=0.7",
    "sphinx-markdown-tables>=0.0.15",
    "sphinx-rtd-theme>=0.5.1",
    "pydata-sphinx-theme>=0.18.0",
    # pin until ubuntu comes with newer pandoc:
    # site-packages/nbsphinx/__init__.py:1058: RuntimeWarning: You are using an unsupported version of pandoc (2.9.2.1).
    # Your version must be at least (2.14.2) but less than (4.0.0).
    "nbsphinx>=0.9.5",
    "pandoc>=2.4",
    "nbconvert>=7.16.4",
    "ipykernel>= 6.23.1",
    "ipython>=7.21.0",
    "sphinx-autodoc-typehints>=3.10",
    # markdown files
    "myst_parser",
]
test = [
    "pytest",
    "pytest-cov",
    "torch"
]
torch = [
    "torch"
]

[project.urls]
Repository = "https://github.com/PEtab-dev/petab_sciml"
"Bug Tracker" = "https://github.com/PEtab-dev/petab_sciml/issues"

[tool.uv.sources]
torch = { index = "pytorch" }

[tool.pytest.ini_options]
testpaths = [
    "tests",
]

[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cpu"
